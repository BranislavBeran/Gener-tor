#include <stdio.h>
#include <stdlib.h>


main(){
	
	int *subor;
	int n;
	char meno[20];
	
	scanf("%s", &meno);
	scanf("d",&n);
	
	subor = (int*)malloc ((4*n+1)*sizeof(unsigned char));
	
}
